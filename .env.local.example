# Copy to .env.local and fill in
GITHUB_TOKEN=                   # PAT, public_repo scope only
NEXT_PUBLIC_SITE_URL=https://bluelayersystems.com
NEXT_PUBLIC_WORKER_URL=         # BLS chat Worker endpoint (falls back to built-in default if unset)
RESEND_API_KEY=                 # for contact form email delivery
